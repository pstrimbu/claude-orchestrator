{
  "name": "claude-orchestrator-v3",
  "version": "0.1.0",
  "main": "dist/main/main.js",
  "scripts": {
    "build": "node generate-version.js && tsc -p tsconfig.main.json && node build-renderer.js",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron .",
    "postinstall": "npx @electron/rebuild --module-dir ."
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-web-links": "^0.11.0",
    "@xterm/xterm": "^5.5.0",
    "@electron/rebuild": "^3.6.0",
    "nanoid": "^5.0.0",
    "node-pty": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "electron": "^34.0.0",
    "typescript": "^5.7.0"
  }
}
